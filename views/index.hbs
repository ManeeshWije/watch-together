<!doctype html>
<html lang="en">
{{> header }}
<body class="bg-dark text-white">
    <div id="swap" class="d-flex flex-column justify-content-center align-items-center">
        <a href="https://github.com/ManeeshWije/watch-together" class="text-white py-4">
            Source Code
        </a>
        {{#if Authenticated }}
            {{> logout }}
            {{> sidebar }}
            {{> video }}
        {{else}}
            {{> login }}
        {{/if}}
        {{> footer}}
    </div>
</body>
<script src="client/client.js"></script>
</html>
